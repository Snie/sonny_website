---
paths:
  - "**/*.md"
---

# Markdown

- After writing or editing a markdown file, run `markdownlint-cli2` and fix all violations before
  moving on. This does not apply to the `specs/` folder.

See `docs/documentation.md` for the documentation architecture (what lives where and why) and the
maintenance workflow.
